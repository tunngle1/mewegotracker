# MeWeGo — Telegram Habit Tracker Bot 🤍

Бот для поддержки пользователей в формировании привычек движения.

## Установка

### 1. Создайте виртуальное окружение

```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте конфигурацию

1. Скопируйте `.env.example` в `.env`:
   ```bash
   copy .env.example .env
   ```

2. Получите токен бота у [@BotFather](https://t.me/BotFather)

3. Вставьте токен в `.env`:
   ```
   BOT_TOKEN=ваш_токен_здесь
   ```

### 4. Запустите бота

```bash
python -m bot.main
```

## Функции

- **Онбординг** — приветственный поток с паузами между сообщениями
- **Выбор привычки** — 5 готовых вариантов + своя
- **Ежедневная отметка** — кнопка "✅ Я здесь"
- **30 уникальных фраз** — разные сообщения каждый день цикла
- **Возвращение после пропуска** — специальные фразы для пользователей, которые пропустили дни
- **Напоминания** — в выбранное время (МСК)
- **Профиль** — просмотр своих данных (/profile)

## Команды бота

- `/start` — начать/перезапустить
- `/profile` — показать профиль
- `/stats` — статистика
- `/cancel` — отменить онбординг

## Структура проекта

```
bot/
├── __init__.py
├── main.py          # Точка входа
├── config.py        # Конфигурация
├── database.py      # База данных
├── models.py        # Модели SQLAlchemy
├── messages.py      # Все тексты и фразы
├── keyboards.py     # Клавиатуры
├── scheduler.py     # Напоминания
└── handlers/
    ├── __init__.py
    ├── onboarding.py  # Онбординг
    ├── tracking.py    # Трекинг привычек
    └── profile.py     # Профиль
```

## Лицензия

MIT
